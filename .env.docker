# Configuración para ejecución en Docker
# Copia estos valores al archivo .env cuando ejecutes Docker

APP_NAME="MS Despacho"
APP_ENV=local
APP_KEY=base64:pFugwnvXRC+bmGB3qEFU5rHOZq76n10/nztC0r83T0Q=
APP_DEBUG=true
APP_URL=http://localhost:8001

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# BASE DE DATOS - Conecta a tu máquina local
# host.docker.internal es la forma de acceder desde un contenedor a localhost
DB_CONNECTION=sqlsrv
DB_HOST=host.docker.internal
DB_PORT=1433
DB_DATABASE=despacho
DB_USERNAME=despacho
DB_PASSWORD=desp@cho1
DB_CHARSET=utf8
DB_ENCRYPT=yes
DB_TRUST_SERVER_CERTIFICATE=true

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=redis
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=file
CACHE_PREFIX=ms_despacho

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Sanctum
SANCTUM_STATEFUL_DOMAINS=localhost:3000,localhost:8000,localhost:8001

# Otros Microservicios
# MS_RECEPCION_URL=http://localhost:8000
# MS_DECISION_URL=http://localhost:8002
# MS_AUTH_URL=http://localhost:8000
# MS_WEBSOCKET_URL=http://localhost:4004
# ML_SERVICE_URL=http://localhost:5000

# GPS Config
GPS_DISTANCE_UNIT=km
GPS_MAX_DISTANCE=50

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0